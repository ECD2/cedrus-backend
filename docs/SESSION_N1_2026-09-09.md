# Session N1 — 2026-09-09 — the brief's identity comes from `user_settings` rows (P1.4, the identity half of B2.1, B2.3, A9)

Worktree `~/Developer/Cedrus/_worktrees/brief-from-settings`, branch `feat/brief-from-settings-2026-09-09` off `main` at `39c6d22`. Bundle 46. Nothing pushed, no migration applied, no database write, no root dependency added (Laws 5, 8). `CEDRUS.md` not edited — proposed lines are under CANON at the bottom.

Read first: `CEDRUS.md` II.0–II.4 in full; II.5's multi-user block (the env-vars-are-secrets-only rule, the `resolveCosUserId` cache bullet, the `user_settings` EMPTY finding), the `COS_USER_ID` bullet and the `cos.owner.derived` bullet; `docs/BUILD_PLAN.md` P1.4, P1.5 (where A9 is named), B2.1, B2.3; `supabase/migrations/20260830120000_multiuser_foundation.sql` §4 (`user_settings`: `cos_user_id` and `usage_user_id`, both nullable, 0 rows in production); the P1.3 session's `docs/BACKFILL_ACCOUNTS_2026-09-09.md` (`bind_cos_identity()` is what will populate the two columns — not applied, not run).

## What landed — two commits, deliberately separable

**Commit 1 — `4d93856` — settings first, env fallback kept and announced. Deployable now.**

- New `src/services/cos/personSettings.js`. `readPersonSettings(userId)`: ONE scoped read of `user_settings` via the Cedrus service client (`lib/supabase.js`), the identity columns only (`user_id, cos_user_id, usage_user_id`), the row or `null`; a read ERROR throws carrying its SQLSTATE (never `null` — "could not read" and "no row" must not collapse, Lesson 1); a blank or missing id is refused (an unscoped read is unexpressible, the `forUser()` posture). `listCosIdentityHolders()`: the `user_id`s whose row has a non-NULL `cos_user_id`. **No module state. No cache of any kind.**
- `resolveCosUserId()` (`writer.js`) gains a `personId` path AHEAD of the env path. The person's own row supplies `cos_user_id` — **source `'settings'`, the label the writeback has reserved since 2026-09-04, true for the first time** — and `usage_user_id` rides along. A missing row or NULL id logs `cos.owner.settings_missing` at warn naming the person (B2.3), an unreadable row logs `cos.owner.settings_unreadable` at error with the SQLSTATE, and only then does it fall to `COS_USER_ID` (`'env'`).
- The job (`cosDailyBrief.js`) finds its PERSON in rows too: `selectPerson()` = the one `user_settings` row that binds a CoS identity. Two such rows ⇒ `cos.brief.aborted` reason `ambiguous_owner` (refuse, never choose — A9; B2.2 turns this into iteration). None ⇒ `cos.owner.settings_missing` (warn) then env. The spend record takes `usage_user_id` from the same row and announces its source (`cos.usage.recorded`, outcome `settings|env`); `defaultLogRun()` is now exported with a `record` seam.
- In production today (`user_settings` empty) every run logs `settings_missing` once and then behaves exactly as before, labelled `env`.

**Commit 2 — (this commit) — the env fallback for `COS_USER_ID` and `COS_BRIEF_USAGE_USER_ID` is REMOVED; a missing row aborts.**

- No row binding a CoS identity ⇒ `cos.owner.settings_missing` at error, then `cos.brief.aborted` reason `settings_missing`, fail closed: nothing gathered, composed, sent, claimed or written. Unreadable ⇒ reason `settings_unreadable`. Two holders ⇒ `ambiguous_owner` (unchanged). A NULL `usage_user_id` ⇒ `cos.usage.unrecorded` (warn) and the run continues, exactly as an unset variable did.
- Also removed, under A9 ("refuses rather than guessing"): the derive-the-owner-from-the-newest-`today_briefs`-row bootstrap path and its `Map` cache in `writer.js`, and with them `resetCosUserId()` and the writer's `cosSelectAcrossAllUsers` import. Once every caller names a person nothing can reach it, and with two people "whoever ran most recently" is a guess. `resolveCosUserId()` with nobody named now refuses (`cos.owner.unresolved`). `cos.owner.derived` and `derived-cached` no longer exist.
- `env` stays in the signatures of `resolveCosUserId()` and `defaultLogRun()` so no call shape changes; NOTHING reads it. The harness restores a read of each and the suites go red.

**The exact statement, for the morning: commit 2 must not merge until `user_settings.cos_user_id` is populated for every active user.** As of the P1.3 session's own read, `user_settings` has 0 rows in production. Deployed before the backfill, every 11:00 UTC tick aborts with `settings_missing` and nobody receives a brief — loudly, but nobody. Commit 1 can merge and deploy on its own; it is what production should run through the backfill, and the first post-backfill 11:00 run under commit 1 is the live proof (`owner id source: settings` in `cos.brief.written`, `usage id source: settings` in `cos.usage.recorded`, no `settings_missing`) before commit 2 is merged.

## Two decisions the prompt did not spell out, made here and stated plainly

1. **How the job knows WHICH person, with no variable and no iteration yet.** The prompt's `readPersonSettings(userId)` needs a person; the scheduler supplies none, and B2.2 (iterate people) is not tonight. The only source that is neither an env var nor a guess is the table itself: the row(s) that bind a CoS identity. Exactly one is the person; zero refuses; two or more refuses. This is not a guess — with one holder there is nothing to choose — and it fails closed the moment a second identity is bound before B2.2 lands, which is the safe direction (a silent choice would be A's brief to B). B2.2 replaces `selectPerson()` with a loop; `resolveCosUserId({ personId })` is already the per-person shape it needs.
2. **Commit 2 removes the derive path, not only the env read.** The prompt names the env fallback; A9 (item 3 of the prompt) is "refuses rather than guessing", and derivation is the guess. It is dead code after commit 2 and it carried the only remaining cache. One revert if Emil disagrees.

## Proof — each with a control, each with a mutation

| Claim | Green | Control | Mutation (harness line) → RED |
|---|---|---|---|
| P1.4: with `COS_USER_ID` and `COS_BRIEF_USAGE_USER_ID` set to nonsense uuids, a present row resolves the row's ids — whose records are gathered, whose ledger slot, whose `today_briefs` row, whose spend (B38 P1.4 section; B46 A9 section) | ✓ | the row's `cos_user_id` blanked: commit 1 resolves the env and says `env`; commit 2 ABORTS (`settings_missing`, nothing gathered/composed/sent/claimed/written, no `cos.brief.written` line) | env read restored ahead of the row; env fallback restored behind it (commit 1's shape); usage fallback restored; the job hands the recorder no row id; zero holders not refused |
| B2.3: a missing row / NULL id / empty table announces itself (`cos.owner.settings_missing`, naming the person where there is one) — warn+fallback in commit 1, error+abort in commit 2 | ✓ | a present row emits neither `settings_missing`, `settings_unreadable` nor an abort | resolver announcement silenced; job announcement silenced |
| unreadable is never missing (SQLSTATE carried) | ✓ | a healthy read emits nothing | `readPersonSettings` returns `null` on error; unreadable listing read as "nobody" |
| A9: A then B in one process differ, each their own; a third person with no row is refused, not A's id; nobody-named is refused, not the env, nothing derived | ✓ | A resolves A (twice, after editing the row between calls — nothing cached) | process-global memo reintroduced on the settings path; `readPersonSettings` forgets its `user_id` filter; holders listing forgets NOT NULL; two holders → first picked; refusal ignored |
| `cos.brief.written` reads `owner id source: settings` when the row supplied the id; `env` when the fallback did (commit 1) | ✓ | a caller-declared label still passes through unchanged | settings path labels itself `env` |

- Bundle 46 `test/person-settings.test.mjs`: 34 PASS (commit 1), 35 PASS (commit 2). Bundle 38 gains a P1.4 section driving the REAL job: 20 PASS (commit 1), 21 PASS (commit 2); whole suite 367 → 368 PASS, and its drop detector confirms no new event lost a field to the logger allowlist (new events use only `outcome`, `reason`, `count`, `error_code`).
- `test/mutate-bundle-46.sh` (runs BOTH suites per mutation): commit 1 **13 RED / 0 missed**, commit 2 **15 RED / 0 missed**, every mutated file restored byte-identical to its pre-run checksum, `trap restore EXIT` present.
- Lesson 4 audit: Bundle 38's owner-source section asserted the old mechanism ("COS_USER_ID set, no user_settings row: the label is env", "no caller id, COS_USER_ID set: userId from env"). Rewritten: the label test is now about a caller-declared label; the env-only test now asserts REFUSAL. `ARMED_ENV.COS_USER_ID` is nonsense suite-wide in commit 2 and every armed run gets one settings row binding `OWNER` through `makeDeps`, so the pre-existing ledger-key assertions built from `OWNER` still discriminate.

## Battery (`sh test/run-all.sh`, this branch)

| State | Result |
|---|---|
| baseline, untouched worktree at `39c6d22` | 35 stages, `ALL BATTERY SUITES PASSED`, exit 0, 12.4 s |
| commit 1 (`4d93856`) | **36 stages, `ALL BATTERY SUITES PASSED`, exit 0, FAIL=0**, 37.7 s |
| commit 2 | 31 stages green, then **Bundle 43 RED (3 FAIL, then the section throws)**, `set -e` stops the battery, exit 1; the stages after it run by hand: Bundle 46 green (35 PASS, exit 0), CoS schema check announces its skip (exit 0), contracts skipped (no toolchain, as on the baseline) |

**Why commit 1's battery is 25 s slower, and why commit 2's is red — the same cause, and it is outside my paths.** `test/programs-foundation.test.mjs` (Bundle 43, R6.2a's file) drives the REAL `runCosDailyBrief()` four times with `env.COS_USER_ID = 'cos-owner'` and injects no settings seam. Under commit 1 each run now reaches the real Cedrus client for `user_settings`, waits ~7 s for `http://supabase.invalid` to fail, announces `settings_unreadable`, and falls back to the env: green, +7 s × 4 (Bundle 43 alone: ~2 s → 15 s measured under commit 2; its runs capture their own console, so the lines do not appear in the battery log). Under commit 2 the same runs ABORT (`settings_unreadable`), which is the correct behaviour of the change and the wrong belief in the test: that suite encodes "COS_USER_ID names the owner" (Lesson 4). I did not edit it (Law 9). The fix, for the merging session, inside `runFor`'s deps (`test/programs-foundation.test.mjs` ~line 473):

```js
        // P1.4: the job finds its person in user_settings rows. One row binding
        // the CoS owner; the fake mirrors Bundle 38's fakeSettingsDb shape.
        settingsDb: { from: () => ({ select: () => { const q = {
          eq: () => q, not: () => q,
          maybeSingle: async () => ({ data: { user_id: A, cos_user_id: 'cos-owner', usage_user_id: null }, error: null }),
          then: (res, rej) => Promise.resolve({ data: [{ user_id: A }], error: null }).then(res, rej),
        }; return q; } }) },
```

With that seam the four runs resolve `'cos-owner'` from the row in milliseconds, the +25 s disappears under commit 1 too, and Bundle 43's own `COS_USER_ID` literal becomes inert (it should then be set to nonsense, as Bundle 38's is).

## Flags for the merging session and for B2.x (Law 9 — noted, not wired)

1. **Bundle 43 patch above** — required for commit 2's battery; recommended alongside commit 1 for the 25 s.
2. **`src/services/programs/today.js` still reads `COS_BRIEF_USAGE_USER_ID`** as `resolveProgramOwner()`'s fallback (R6.2a's file, not mine). After the backfill its rows-first lookup succeeds for Emil and the read is unreachable in practice, but it exists. The job now KNOWS the `app_users.id` (`who.personId`) before it calls `programOwner`, so the reverse lookup by `cos_user_id` is redundant: B2.1 should pass `personId` straight through and delete the fallback. Until then "no environment variable names a person" is true of the brief's identity and spend, and not yet of the today's-program block's fallback.
3. **Railway variables.** After commit 2 merges, `COS_USER_ID` and `COS_BRIEF_USAGE_USER_ID` are unread by the brief and can be removed from the service — Emil's action (Law 5), and only after flag 2 for the usage one.
4. **`scripts/verify-brief-run.mjs`'s header** still says `cos.owner.derived` "sits between cos.send.ok and cos.brief.written on every real run" — false since 2026-08-30 (II.5 already says so) and the event no longer exists after commit 2. The verifier does not require it (Bundle 40 stays green). Not my file.
5. **Scheduler comment** on `cos-daily-brief` ("a single-owner job") stays true until B2.2.
6. **This file's name** was given literally as `SESSION_N1_2026-09-09.md` in a rules block shared by every session tonight; if siblings used the same name the morning fold will see a collision — rename freely, nothing links to it.

## Post-deploy verification of commit 1 (Law 10)

At the next 11:00 UTC tick after the backfill: `cos.brief.written` carries `owner id source: settings`; `cos.usage.recorded` carries `usage id source: settings`; no `cos.owner.settings_missing`. Before the backfill: exactly one `cos.owner.settings_missing` (warn, outcome `no_row`) per run, then `owner id source: env` — that line is the proof the fallback, not the row, still names Emil, and it is what commit 2 turns into an abort.

## CANON

Proposed II.5 lines (fold in, do not paste verbatim if a bullet already covers it):

- **`user_settings` IS read now** (P1.4, 2026-09-09, commits `4d93856` and the one after it). The 2026-09-04 bullet's "Nothing in `src/` reads the `user_settings` table" is false from commit 1: `src/services/cos/personSettings.js` is the one reader, one scoped read per person, no cache. `owner id source: settings` in `cos.brief.written` is TRUE when it appears — it can appear only when the person's row supplied the id.
- **The brief finds its person in rows: the one `user_settings` row whose `cos_user_id` is bound.** Two bound rows abort the run (`ambiguous_owner`) until B2.2 iterates — fail closed, never choose. Zero bound rows: commit 1 announces and falls back to `COS_USER_ID`; commit 2 aborts (`cos.brief.aborted`, reason `settings_missing`). **Commit 2 must not merge until `user_settings.cos_user_id` is populated for every active user.**
- **`cos.owner.derived` no longer exists** (commit 2). The derive-from-newest-`today_briefs`-row bootstrap and its `Map` cache are gone under A9; `resolveCosUserId()` with nobody named refuses (`cos.owner.unresolved`). The `resolveCosUserId` cache bullet becomes history: there is no owner slot of any kind in `writer.js`.
- **`COS_USER_ID` and `COS_BRIEF_USAGE_USER_ID` are unread by the brief from commit 2** and can leave Railway after it deploys. `src/services/programs/today.js` still reads the usage one as a fallback (flag 2 above); the per-person rule is not fully closed until that goes.
- **New events:** `cos.owner.settings_missing` (outcome `no_row` | `null_id`; warn in commit 1, error in commit 2), `cos.owner.settings_unreadable` (error, `error_code` = SQLSTATE), `cos.usage.recorded` (outcome `settings`), `cos.brief.aborted` now carries `reason` (`settings_missing` | `settings_unreadable` | `ambiguous_owner` | `no_user_id`).
- **Lesson candidate (Lesson 4 in a new place): a sibling suite that drives your REAL entry point encodes your old mechanism.** Bundle 43 runs the real job with `COS_USER_ID` and no settings seam; retiring the variable could not be done without that file going red, and Law 9 forbade touching it from here. When a change retires a mechanism, grep the OTHER bundles for the real entry point, not only your own.
- **A read against `http://supabase.invalid` costs ~7 s** (measured 2026-09-09, 7.0 s: supabase-js resolves an error, it does not throw). Any real-job run in a suite that does not inject every database seam pays it silently — the battery went 12 s → 38 s under commit 1 with no red anywhere, which is how a suite "that is green but takes minutes stops being run".
