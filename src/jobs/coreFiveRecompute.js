import { logger } from '../utils/logger.js';
// import * as coreFive from '../services/coreFive.js';

// Monthly: re-score each active user's circle and swap with hysteresis.
export async function runMonthlyCoreFive() {
  // TODO: iterate active users -> coreFive.recomputeCoreFive(userId, { reason: 'monthly_recompute' })
  logger.info('runMonthlyCoreFive tick — not yet implemented');
}
